version: '2'

linters:
  default: none
  enable:
    - govet
    - gocritic

  settings:
    gocritic:
      disabled-checks:
        - singleCaseSwitch

formatters:
  enable:
    - gofmt

  settings:
    gofmt:
      simplify: false
